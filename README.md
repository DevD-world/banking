# Banking

Application Completion Intelligence Platform for banking onboarding, document OCR, validation, workflow tracking, and CRM submission.

## Render

Build command:

```bash
pip install -r requirements.txt
```

Start command:

```bash
python backend/server.py
```
